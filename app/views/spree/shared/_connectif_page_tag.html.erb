<div class="cn_page_tag" style="display:none"><%= "#{action_name.humanize} #{controller_name.humanize.downcase} ""#{@product.present? ? @product.name : nil}"" #{@taxon.present? && @product.blank? ? @taxon.name.downcase : nil}""#{@page.present? ? @page.title : nil}""#{(@order.present? && controller_name == 'checkout')  ? @order.state : nil}" %></div>